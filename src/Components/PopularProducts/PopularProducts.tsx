import s from './PopularProducts.module.scss';

const PopularProducts: React.FC<any> = (props) => {
    return (
        <div className={s.popular_products} >
            
        </div>
    )
}

export default PopularProducts;